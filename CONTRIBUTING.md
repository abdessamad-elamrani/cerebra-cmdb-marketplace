# Contributing

1. Create or update one pack under `packs/<packId>/versions/<version>/`.
2. Update semver when content changes.
3. Ensure profile JSON is valid and graph filenames map to command IDs.
4. Open a PR.
